function openPopup() {
  // Specify the URL of the pop-up window
  const url = 'mexicana.html';

  // Specify the size and position of the pop-up window
  const width = 700;
  const height = 430;
  const left = (window.innerWidth - width) / 2;
  const top = (window.innerHeight - height) / 2;

  // Open the pop-up window
    window.open(url, '_blank', `width=${width}, height=${height}, left=${left}, top=${top}, resizable=no`);
}


function openPopup2() {
  // Specify the URL of the pop-up window
  const url = 'americana.html';

  // Specify the size and position of the pop-up window
  const width = 700;
  const height = 430;
  const left = (window.innerWidth - width) / 2;
  const top = (window.innerHeight - height) / 2;

  // Open the pop-up window
    window.open(url, '_blank', `width=${width}, height=${height}, left=${left}, top=${top}, resizable=no`);
}

function openPopup3() {
  // Specify the URL of the pop-up window
  const url = 'hawaiian.html';

  // Specify the size and position of the pop-up window
  const width = 700;
  const height = 430;
  const left = (window.innerWidth - width) / 2;
  const top = (window.innerHeight - height) / 2;

  // Open the pop-up window
    window.open(url, '_blank', `width=${width}, height=${height}, left=${left}, top=${top}, resizable=no`);
}


function openPopup4() {
  // Specify the URL of the pop-up window
  const url = 'hot_burger.html';

  // Specify the size and position of the pop-up window
  const width = 700;
  const height = 430;
  const left = (window.innerWidth - width) / 2;
  const top = (window.innerHeight - height) / 2;

  // Open the pop-up window
    window.open(url, '_blank', `width=${width}, height=${height}, left=${left}, top=${top}, resizable=no`);
}